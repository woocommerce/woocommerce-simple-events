<?php
declare(strict_types=1);

// THIS FILE IS AUTO-GENERATED. DO NOT EDIT MANUALLY.

namespace Automattic\WooCommerceSimpleEvents\Internal\Api\Autogenerated\GraphQLMutations;

use Automattic\WooCommerceSimpleEvents\Api\Mutations\UpdateEvent as UpdateEventCommand;
use Automattic\WooCommerce\Api\Infrastructure\QueryInfoExtractor;
use Automattic\WooCommerce\Api\Infrastructure\ResolverHelpers;
use Automattic\WooCommerceSimpleEvents\Internal\Api\Autogenerated\GraphQLTypes\Output\Event as EventType;
use Automattic\WooCommerceSimpleEvents\Internal\Api\Autogenerated\GraphQLTypes\Input\UpdateEvent as UpdateEventInput;
use Automattic\WooCommerce\Api\Infrastructure\Schema\ResolveInfo;
use Automattic\WooCommerce\Api\Infrastructure\Schema\Type;

class UpdateEvent {
	public static function get_field_definition(): array {
		return array(
			'type'          => Type::nonNull( EventType::get() ),
			'description'   => __( 'Patch an existing event.', 'woocommerce-simple-events' ),
			'authorization' => array(
				array(
					'attribute' => 'PublicAccess',
					'args'      => array(),
				),
			),
			'args'          => array(
				'id'    => array(
					'type'        => Type::nonNull( Type::int() ),
					'description' => __( 'Identifier of the event to patch.', 'woocommerce-simple-events' ),
				),
				'input' => array(
					'type' => Type::nonNull( UpdateEventInput::get() ),
				),
			),
			'resolve'       => array( self::class, 'resolve' ),
		);
	}

	public static function resolve( mixed $root, array $args, mixed $context, ResolveInfo $info ): mixed {
		// Publish the root query's metadata so downstream field-level
		// authorization gates can read it via `$_metadata['query']`.
		// $context is an ArrayObject (see GraphQLController::process_request())
		// so the mutation propagates to nested resolvers.
		$context['_query_metadata'] = array();

		$command = \Automattic\WooCommerceSimpleEvents\Api\Infrastructure\ClassResolver::resolve_class( UpdateEventCommand::class );

		$execute_args = array();
		if ( array_key_exists( 'id', $args ) ) {
			$execute_args['id'] = $args['id'];
		}
		if ( array_key_exists( 'input', $args ) ) {
			$execute_args['input'] = self::convert_update_event_input( $args['input'] );
		}

		if ( isset( $execute_args['input'] ) && $execute_args['input'] instanceof \Automattic\WooCommerceSimpleEvents\Api\InputTypes\UpdateEventInput ) {
			$_parent = $execute_args['input'];
			if ( $_parent->was_provided( 'name' ) ) {
				$principal = $context['principal'];
				$_metadata = array(
					'query' => $context['_query_metadata'] ?? array(),
					'type'  => array(),
					'field' => array(),
				);
				$_args     = $args;
				if ( ! ( ( new \Automattic\WooCommerceSimpleEvents\Api\Attributes\RequiresScope( 'events:edit' ) )->authorize( $principal ) ) ) {
					throw ResolverHelpers::build_field_authorization_error( $principal, 'UpdateEventInput', 'name', 'RequiresScope' );
				}
			}
			if ( $_parent->was_provided( 'capacity' ) ) {
				$principal = $context['principal'];
				$_metadata = array(
					'query' => $context['_query_metadata'] ?? array(),
					'type'  => array(),
					'field' => array(),
				);
				$_args     = $args;
				if ( ! ( ( new \Automattic\WooCommerceSimpleEvents\Api\Attributes\RequiresScope( 'events:edit' ) )->authorize( $principal ) ) ) {
					throw ResolverHelpers::build_field_authorization_error( $principal, 'UpdateEventInput', 'capacity', 'RequiresScope' );
				}
			}
			if ( $_parent->was_provided( 'waitlist_size' ) ) {
				$principal = $context['principal'];
				$_metadata = array(
					'query' => $context['_query_metadata'] ?? array(),
					'type'  => array(),
					'field' => array(),
				);
				$_args     = $args;
				if ( ! ( ( new \Automattic\WooCommerceSimpleEvents\Api\Attributes\RequiresScope( 'events:edit' ) )->authorize( $principal ) ) ) {
					throw ResolverHelpers::build_field_authorization_error( $principal, 'UpdateEventInput', 'waitlist_size', 'RequiresScope' );
				}
			}
			if ( $_parent->was_provided( 'internal_notes' ) ) {
				$principal = $context['principal'];
				$_metadata = array(
					'query' => $context['_query_metadata'] ?? array(),
					'type'  => array(),
					'field' => array(),
				);
				$_args     = $args;
				if ( ! ( ( new \Automattic\WooCommerceSimpleEvents\Api\Attributes\RequiresScope( 'events:edit' ) )->authorize( $principal ) ) ) {
					throw ResolverHelpers::build_field_authorization_error( $principal, 'UpdateEventInput', 'internal_notes', 'RequiresScope' );
				}
			}
		}

		$result = ResolverHelpers::execute_command( $command, $execute_args );

		return $result;
	}

	/**
	 * Compute the value `_preauthorized` would carry for a given principal —
	 * the AND of the autodiscovered authorization attributes' authorize()
	 * outcomes on this command. Single source of truth for both the resolver's
	 * own gates and external (code-API) callers asking about authorization
	 * without going through GraphQL execution.
	 *
	 * Returns true vacuously when the command has no authorization attributes
	 * (in that case authorize() on the command is the sole guard, and that
	 * method should be consulted instead).
	 */
	public static function compute_preauthorized( \Automattic\WooCommerceSimpleEvents\Api\Infrastructure\EventsPrincipal $principal ): bool {
		return true;
	}

	private static function convert_update_event_input( array $data ): \Automattic\WooCommerceSimpleEvents\Api\InputTypes\UpdateEventInput {
		$input = new \Automattic\WooCommerceSimpleEvents\Api\InputTypes\UpdateEventInput();

		if ( array_key_exists( 'name', $data ) ) {
			$input->mark_provided( 'name' );
				$input->name = $data['name'];
		}
		if ( array_key_exists( 'capacity', $data ) ) {
			$input->mark_provided( 'capacity' );
					$input->capacity = $data['capacity'];
		}
		if ( array_key_exists( 'waitlist_size', $data ) ) {
			$input->mark_provided( 'waitlist_size' );
					$input->waitlist_size = $data['waitlist_size'];
		}
		if ( array_key_exists( 'internal_notes', $data ) ) {
			$input->mark_provided( 'internal_notes' );
					$input->internal_notes = $data['internal_notes'];
		}

		return $input;
	}
}
