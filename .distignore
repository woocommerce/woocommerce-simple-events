# Excluded from release builds (composer build:zip, wp dist-archive).
# The generated GraphQL layer (src/Internal/Api/Autogenerated/) is committed
# and ships as-is, so the development-only build tooling is left out.
/.ai
/.claude
/.git
/.github
/bin
/dist
/node_modules
/tests
/tmp
/.distignore
/.gitignore
/composer.lock
/package.json
/pnpm-lock.yaml
